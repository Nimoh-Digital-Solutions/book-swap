/**
 * rating.types.ts
 *
 * Re-exports from `@shared` — see `packages/shared/src/types/rating.ts`
 * for the canonical contracts (the BookSwap source of truth shared with the
 * mobile app).
 */

export type {
  ExchangeRatingStatus,
  PaginatedRatings,
  Rating,
  RatingUser,
  SubmitRatingPayload,
} from '@shared/types/rating';
